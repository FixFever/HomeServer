services:

  traefik:
    image: traefik:latest
    container_name: traefik
    network_mode: host
    environment:
      - TLS_EMAIL=${TLS_EMAIL}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_data:/etc/traefik
      - traefik_certs:/letsencrypt
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=host
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --providers.file.filename=/etc/traefik/dynamic.yml
      - --providers.file.watch=true
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
      - --certificatesresolvers.letsencrypt.acme.tlschallenge=true
      - --certificatesresolvers.letsencrypt.acme.email=${TLS_EMAIL}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --log.level=INFO
      - --accesslog=true      
      # - --api.dashboard=true
      # - --api.insecure=true
      # - --entrypoints.dashboard.address=:8082
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Traefik
      homepage.icon: traefik.png

  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    networks:
      - watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /etc/localtime:/etc/localtime:ro
    environment:
      WATCHTOWER_CLEANUP: true
      WATCHTOWER_DISABLE_CONTAINERS: /nextcloud /nextcloud_cron /mediacms /celery /celery_beat /migrations
      WATCHTOWER_NOTIFICATIONS: shoutrrr
      WATCHTOWER_NOTIFICATION_URL: telegram://${TELEGRAM_BOT_TOKEN}@telegram?chats=${CHAT_ID}
      DOCKER_API_VERSION: 1.49
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Watchtower
      homepage.icon: watchtower.png

  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    ports:
      - 3005:3000
    environment:
      HOMEPAGE_ALLOWED_HOSTS: ${DOMAIN},localhost:3005
    networks:
      - apps
    volumes:
      - homepage:/app/config
      - homepage_images:/app/public/images
      - homepage_icons:/app/public/icons
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.homepage.rule: Host(`${DOMAIN}`)
      traefik.http.routers.homepage.entrypoints: websecure
      traefik.http.routers.homepage.tls.certresolver: letsencrypt
      traefik.http.services.homepage.loadbalancer.server.port: 3000
      homepage.group: Containers
      homepage.name: Homepage
      homepage.icon: homepage.png
      
# For update: docker-compose build nextcloud
  nextcloud:
    build:
      context: ./nextcloud/
      args:
        NEXTCLOUD_IMAGE: nextcloud:32
    container_name: nextcloud
    ports:
      - 5000:80
    environment:
      - POSTGRES_HOST=postgres:5432
      - POSTGRES_DB=${NEXTCLOUD_POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - NEXTCLOUD_ADMIN_USER=${NEXTCLOUD_ADMIN_USER}
      - NEXTCLOUD_ADMIN_PASSWORD=${NEXTCLOUD_ADMIN_PASSWORD}
      - REDIS_HOST=redis
      - PHP_MEMORY_LIMIT=3072M
      - APACHE_BODY_LIMIT=0
    depends_on:
      - postgres
      - redis
    networks:
      - apps
    volumes:
      - nextcloud_html:/var/www/html
      - C:\git\server\nextcloud\php.ini:/usr/local/etc/php/conf.d/php.ini
      - C:\\docker-caches\nextcloud\preview:/var/www/html/data/appdata_ocah283m7og5/preview
      - M:\\nextcloud\FixFever:/var/www/html/data/FixFever/files
      - M:\\nextcloud\Elya:/var/www/html/data/Elya/files
      - M:\\nextcloud\Boris:/var/www/html/data/Boris/files
      - M:\\nextcloud\Marina:/var/www/html/data/Marina/files
      - M:\\nextcloud\trashbin\FixFever:/var/www/html/data/FixFever/files_trashbin/files
      - M:\\nextcloud\trashbin\Elya:/var/www/html/data/Elya/files_trashbin/files
      - M:\\nextcloud\trashbin\Boris:/var/www/html/data/Boris/files_trashbin/files
      - M:\\nextcloud\trashbin\Marina:/var/www/html/data/Marina/files_trashbin/files
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.nextcloud.rule: Host(`nextcloud.${DOMAIN}`)
      traefik.http.routers.nextcloud.entrypoints: websecure
      traefik.http.routers.nextcloud.tls.certresolver: letsencrypt
      traefik.http.services.nextcloud.loadbalancer.server.port: 80
      homepage.group: Cloud
      homepage.name: Nextcloud
      homepage.icon: nextcloud.png
      homepage.href: https://nextcloud.${DOMAIN}
      homepage.description: File-hosting service

  postgres:
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    networks:
      - apps
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: PostgreSQL
      homepage.icon: postgres.png

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - 6379:6379
    networks:
      - apps
    volumes:
      - redis_data:/data
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Redis
      homepage.icon: redis.png

  nextcloud_cron:
    image: nextcloud:32
    container_name: nextcloud_cron
    volumes_from:
      - nextcloud:rw
    entrypoint: /cron.sh
    user: 0:0
    depends_on:
      - postgres
    networks:
      - apps
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Nextcloud cron
      homepage.icon: nextcloud.png

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    ports:
      - 5050:80
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
      - POSTGRES_DB=${NEXTCLOUD_POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    depends_on:
      - postgres
    networks:
      - apps
    volumes:
      - pgadmin:/var/lib/pgadmin
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.pgadmin.rule: Host(`pgadmin.${DOMAIN}`)
      traefik.http.routers.pgadmin.entrypoints: websecure
      traefik.http.routers.pgadmin.tls.certresolver: letsencrypt
      traefik.http.services.pgadmin.loadbalancer.server.port: 80
      homepage.group: Utilities
      homepage.name: pgAdmin
      homepage.icon: pgadmin.png
      homepage.href: https://pgadmin.${DOMAIN}
      homepage.description: PostgreSQL management tool

  immich:
    container_name: immich
    image: ghcr.io/immich-app/immich-server:release
    ports:
      - 2283:2283
    volumes:
      - M:\\immich:/data
      - C:\\docker-caches\immich\encoded-video:/data/encoded-video
      - C:\\docker-caches\immich\thumbs:/data/thumbs
      - C:\\docker-caches\immich\upload:/data/upload
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: Europe/Moscow
      DB_HOSTNAME: postgres
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_DATABASE_NAME: immich
    depends_on:
      - redis
      - postgres
      - immich-machine-learning
    networks:
      - apps
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.immich.rule: Host(`immich.${DOMAIN}`)
      traefik.http.routers.immich.entrypoints: websecure
      traefik.http.routers.immich.tls.certresolver: letsencrypt
      traefik.http.services.immich.loadbalancer.server.port: 2283
      homepage.group: Cloud
      homepage.name: Immich
      homepage.icon: immich.png
      homepage.href: https://immich.${DOMAIN}
      homepage.description: Photo and video management solution
      homepage.widget.type: immich
      homepage.widget.fields: '["photos", "videos", "storage"]'
      homepage.widget.url: http://immich:2283
      homepage.widget.version: 2
      homepage.widget.key: ${IMMICH_API_KEY}

  immich-machine-learning:
    container_name: immich-machine-learning
    image: ghcr.io/immich-app/immich-machine-learning:release
    networks:
      - apps
    volumes:
      - immich-model-cache:/cache
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Immich machine learning
      homepage.icon: immich.png

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - '--storage.tsdb.retention.time=3y'
    ports:
      - 9090:9090
    volumes:
      - prometheus_etc:/etc/prometheus
      - H:\docker-volumes\prometheus:/prometheus
    environment:
      TZ: "Europe/Moscow"
    networks:
      - monitoring
    restart: unless-stopped
    labels:
      # traefik.http.routers.prometheus.rule: Host(`prometheus.${DOMAIN}`)
      # traefik.http.routers.prometheus.entrypoints: websecure
      # traefik.http.routers.prometheus.tls.certresolver: letsencrypt
      # traefik.http.routers.prometheus.middlewares: prometheus-basic-auth
      # traefik.http.services.prometheus.loadbalancer.server.port: 9090
      homepage.group: Monitoring
      homepage.name: Prometheus
      homepage.icon: prometheus.png
      homepage.href: https://prometheus.${DOMAIN}
      homepage.description: Monitoring

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - 3000:3000
    depends_on:
      - prometheus
    networks:
      - monitoring
    volumes:
      - grafana_lib:/var/lib/grafana
      - grafana_provisioning:/etc/grafana/provisioning/
    environment:
      TZ: "Europe/Moscow"
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
      GF_USERS_ALLOW_SIGN_UP: false
      GF_SERVER_ROOT_URL: https://grafana.${DOMAIN}
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.grafana.rule: Host(`grafana.${DOMAIN}`)
      traefik.http.routers.grafana.entrypoints: websecure
      traefik.http.routers.grafana.tls.certresolver: letsencrypt
      traefik.http.services.grafana.loadbalancer.server.port: 3000
      homepage.group: Monitoring
      homepage.name: Grafana
      homepage.icon: grafana.png
      homepage.href: https://grafana.${DOMAIN}
      homepage.description: Data visualization & Monitoring

  secret:
    image: ghcr.io/tethik/burn-after-reading/burn-after-reading:1.5.1
    container_name: secret
    ports:
      - 5035:80
    volumes:
      - secret:/opt/data
    networks:
      - secret
    environment:
      - MAX_CONTENT_LENGTH=134217728
      - BURN_DATA_PATH=/opt/data/
      - BURN_MAX_STORAGE=1024
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.secret.rule: Host(`secret.${DOMAIN}`)
      traefik.http.routers.secret.entrypoints: websecure
      traefik.http.routers.secret.tls.certresolver: letsencrypt
      traefik.http.services.secret.loadbalancer.server.port: 80
      homepage.group: Utilities
      homepage.name: Burn after reading
      homepage.icon: /icons/secret.png
      homepage.href: https://secret.${DOMAIN}
      homepage.description: Share short term messages stored in memory

  vault:
    image: vaultwarden/server:latest
    container_name: vault
    ports:
      - 3012:3012
      - 8282:80
    networks:
      - vault
    volumes:
      - vault:/data
    environment:
      - WEBSOCKET_ENABLED=true
      - LOG_FILE=/data/logs.txt
      - ADMIN_TOKEN=${VAULT_ADMIN_TOKEN}
      - DOMAIN=https://vault.${DOMAIN}
      - SIGNUPS_ALLOWED=true
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.vault.rule: Host(`vault.${DOMAIN}`)
      traefik.http.routers.vault.entrypoints: websecure
      traefik.http.routers.vault.tls.certresolver: letsencrypt
      traefik.http.services.vault.loadbalancer.server.port: 80
      homepage.group: Cloud
      homepage.name: Vaultwarden
      homepage.icon: vaultwarden.png
      homepage.href: https://vault.${DOMAIN}
      homepage.description: Password management

  gokapi:
    image: f0rc3/gokapi:latest
    container_name: gokapi
    ports:
      - 8083:53842
    networks:
      - gokapi
    volumes:
      - gokapi_config:/app/config
      - gokapi_data:/app/data
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.gokapi.rule: Host(`gokapi.${DOMAIN}`)
      traefik.http.routers.gokapi.entrypoints: websecure
      traefik.http.routers.gokapi.tls.certresolver: letsencrypt
      traefik.http.services.gokapi.loadbalancer.server.port: 53842
      homepage.group: Utilities
      homepage.name: Gokapi
      homepage.icon: /icons/gokapi.png
      homepage.href: https://gokapi.${DOMAIN}
      homepage.description: Share files

  chrome:
    image: vital987/chrome-novnc:latest
    container_name: chrome
    ports:
      - 9870:9870
    environment:
      PORT: 9870
      VNC_PASS: ${CHROME_PASSWORD}
    networks:
      - chrome
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.chrome.rule: Host(`chrome.${DOMAIN}`)
      traefik.http.routers.chrome.entrypoints: websecure
      traefik.http.routers.chrome.tls.certresolver: letsencrypt
      traefik.http.services.chrome.loadbalancer.server.port: 9870
      homepage.group: Utilities
      homepage.name: Chrome
      homepage.icon: chrome.png
      homepage.href: https://chrome.${DOMAIN}
      homepage.description: Chrome noVNC

  notes:
    image: standardnotes/web:latest
    container_name: notes
    ports:
      - 3003:80
    networks:
      - notes
    volumes:
      - notes_html:/usr/share/nginx/html # Env var DEFAULT_SYNC_SERVER is not supported in web app docker image.
                                         # To change default url of standard notes api, in /usr/share/nginx/html/app.js:
                                         # replace
                                         # function(e){e.Api="https://api.standardnotes.com"
                                         # to
                                         # function(e){e.Api="https://your.notes.api.domain.com"
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.notes.rule: Host(`notes.${DOMAIN}`)
      traefik.http.routers.notes.entrypoints: websecure
      traefik.http.routers.notes.tls.certresolver: letsencrypt
      traefik.http.services.notes.loadbalancer.server.port: 80
      homepage.group: Cloud
      homepage.name: Standard notes
      homepage.icon: /icons/notes.png
      homepage.href: https://notes.${DOMAIN}
      homepage.description: Note-taking app

  notes_api:
    image: standardnotes/server:latest
    container_name: notes_api
    depends_on:
      - localstack
      - mysql
    ports:
      - 3002:3000
      - 3125:3104
    networks:
      - apps
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USERNAME=${NOTES_DB_USERNAME}
      - DB_PASSWORD=${NOTES_DB_PASSWORD}
      - DB_DATABASE=${NOTES_DB_NAME}
      - DB_TYPE=mysql
      - REDIS_PORT=6379
      - REDIS_HOST=redis
      - CACHE_TYPE=redis
      - AUTH_JWT_SECRET=${NOTES_AUTH_JWT_SECRET}
      - AUTH_SERVER_ENCRYPTION_SERVER_KEY=${NOTES_AUTH_SERVER_ENCRYPTION_SERVER_KEY}
      - VALET_TOKEN_SECRET=${NOTES_VALET_TOKEN_SECRET}
      - DISABLE_USER_REGISTRATION=true
      - COOKIE_DOMAIN=api.notes.${DOMAIN}
    volumes:
      - notes_logs:/var/lib/server/logs
      - notes_uploads:/opt/server/packages/files/dist/uploads
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.notes_api.rule: Host(`api.notes.${DOMAIN}`)
      traefik.http.routers.notes_api.entrypoints: websecure
      traefik.http.routers.notes_api.tls.certresolver: letsencrypt
      traefik.http.services.notes_api.loadbalancer.server.port: 3000
      homepage.group: Containers
      homepage.name: Standard notes api
      homepage.icon: https://notes.${DOMAIN}/favicon/apple-touch-icon.png

  localstack:
    image: localstack/localstack:3.0
    container_name: localstack
    expose:
      - 4566
    environment:
      - SERVICES=sns,sqs
      - HOSTNAME_EXTERNAL=localstack
      - LS_LOG=warn
    volumes:
      - localstack:/var/lib/localstack
      - localstack_etc:/etc/localstack/init/ready.d # chmod +x /etc/localstack/init/ready.d/localstack_bootstrap.sh
    networks:
      - apps
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: Localstack
      homepage.icon: /icons/localstack.png

  mysql:
    image: mysql
    container_name: mysql
    environment:
      - MYSQL_DATABASE=${NOTES_DB_NAME}
      - MYSQL_USER=${NOTES_DB_USERNAME}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_PASSWORD=${NOTES_DB_PASSWORD}
    ports:
      - 3306:3306
    networks:
      - apps
    volumes:
      - mysql:/var/lib/mysql
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: MySQL
      homepage.icon: mysql.png

  cobalt_api:
    image: ghcr.io/imputnet/cobalt:latest
    container_name: cobalt_api
    ports:
      - 9002:9000/tcp
    networks:
      - cobalt_api
    volumes: 
      # youtube download fix: 
      # Add cookie to the youtube_oauth array in cookies.json file
      # cd cobalt
      # npm install
      # npm run token:youtube
      # And fix /app/src/modules/processing/services/youtube.js:
      # change `info = await yt.getBasicInfo(o.id, yt.session.logged_in ? 'ANDROID' : 'IOS');`
      # to     `info = await yt.getBasicInfo(o.id, 'ANDROID');`
      # I dont know why, but it works
      # manual: https://github.com/imputnet/cobalt/blob/main/docs/configure-for-youtube.md
      - cobalt_api_cookies:/cookies
      - cobalt_api_services:/app/src/modules/processing/services
    environment:
      - API_URL=https://api.cobalt.${DOMAIN}/
      - RATELIMIT_WINDOW=21600
      - RATELIMIT_MAX=1000
      - DURATION_LIMIT=21600
      - COOKIE_PATH=cookies/cookies.json
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.cobalt_api.rule: Host(`api.cobalt.${DOMAIN}`)
      traefik.http.routers.cobalt_api.entrypoints: websecure
      traefik.http.routers.cobalt_api.tls.certresolver: letsencrypt
      traefik.http.services.cobalt_api.loadbalancer.server.port: 9000
      homepage.group: Containers
      homepage.name: Cobalt api
      homepage.icon: /icons/cobalt.png

  speedtest:
    image: ghcr.io/librespeed/speedtest:latest
    container_name: speedtest
    ports:
      - 5444:8080
    networks:
      - speedtest
    environment:
      - MODE=standalone
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.speedtest.rule: Host(`speedtest.${DOMAIN}`)
      traefik.http.routers.speedtest.entrypoints: websecure
      traefik.http.routers.speedtest.tls.certresolver: letsencrypt
      traefik.http.services.speedtest.loadbalancer.server.port: 8080
      homepage.group: Utilities
      homepage.name: LibreSpeed
      homepage.icon: librespeed.png
      homepage.href: https://speedtest.${DOMAIN}
      homepage.description: Speed test

  it-tools:
    image: corentinth/it-tools:latest
    container_name: it-tools
    ports:
      - 5445:80
    networks:
      - it-tools
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.it_tools.rule: Host(`it-tools.${DOMAIN}`)
      traefik.http.routers.it_tools.entrypoints: websecure
      traefik.http.routers.it_tools.tls.certresolver: letsencrypt
      traefik.http.services.it_tools.loadbalancer.server.port: 80
      homepage.group: Utilities
      homepage.name: IT-tools
      homepage.icon: it-tools.png
      homepage.href: https://it-tools.${DOMAIN}
      homepage.description: Useful tools for developer and people working in IT

  pdf:
    image: frooodle/s-pdf:latest
    container_name: pdf
    ports:
      - 8085:8080
    networks:
      - pdf
    environment:
      - SECURITY_ENABLELOGIN=true
      - LANGS=en,ru
      - OCR_LANGS=eng,rus
    volumes:
      - stirling_tessdata:/usr/share/tessdata  # OCR language files
      - stirling_configs:/configs               # Settings & database
      - stirling_logs:/logs                     # Application logs
      - stirling_pipeline:/pipeline             # Automation configs
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.pdf.rule: Host(`pdf.${DOMAIN}`)
      traefik.http.routers.pdf.entrypoints: websecure
      traefik.http.routers.pdf.tls.certresolver: letsencrypt
      traefik.http.services.pdf.loadbalancer.server.port: 8080
      homepage.group: Utilities
      homepage.name: Stirling PDF
      homepage.icon: stirling-pdf
      homepage.href: https://pdf.${DOMAIN}
      homepage.description: PDF editing platform

  # https://learn.microsoft.com/ru-ru/windows/wsl/connect-usb#attach-a-usb-device
  co2-exporter:
    image: huhamhire/air-co2-exporter:latest
    container_name: co2-exporter
    ports:
      - 9110:9110
    networks:
      - monitoring
    privileged: true
    restart: unless-stopped
    labels:
      homepage.group: Containers
      homepage.name: CO2 meter exporter
      homepage.icon: docker

  shinobi:
    image: shinobisystems/shinobi:latest
    container_name: shinobi
    ports:
      - 5009:8080
    networks:
      - shinobi
    environment:
      - TZ=Europe/Moscow
    volumes:
      - shinobi:/home/Shinobi
      - shinobi_custom_auto_load:/customAutoLoad
      - shinobi_plugins:/home/Shinobi/plugins
      - shinobi_config:/config
      - shinobi_database:/var/lib/mysql
      - H:\shinobi:/home/Shinobi/videos
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.shinobi.rule: Host(`shinobi.${DOMAIN}`)
      traefik.http.routers.shinobi.entrypoints: websecure
      traefik.http.routers.shinobi.tls.certresolver: letsencrypt
      traefik.http.services.shinobi.loadbalancer.server.port: 8080
      homepage.group: Utilities
      homepage.name: Shinobi
      homepage.icon: shinobi
      homepage.href: https://shinobi.${DOMAIN}
      homepage.description: CCTV and NVR

  homeassistant:
    container_name: homeassistant
    image: ghcr.io/home-assistant/home-assistant:latest
    network_mode: host
    volumes:
      - homeassistant:/config
      - C:/git/server/homeassistant:/server
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=Europe/Moscow
    command: "/server/start_with_hacs.sh"
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.homeassistant.rule: Host(`homeassistant.${DOMAIN}`)
      traefik.http.routers.homeassistant.entrypoints: websecure
      traefik.http.routers.homeassistant.tls.certresolver: letsencrypt
      traefik.http.services.homeassistant.loadbalancer.server.port: 8123
      homepage.group: Utilities
      homepage.name: Home assistant
      homepage.icon: home-assistant
      homepage.href: https://homeassistant.${DOMAIN}
      homepage.description: Home automation service

  opentracker:
    container_name: opentracker
    image: lednerb/opentracker-docker
    ports:
      - 6969:6969
      - 6969:6969/udp
    networks:
      - opentracker
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.opentracker.rule: Host(`tracker.${DOMAIN}`)
      traefik.http.routers.opentracker.entrypoints: websecure
      traefik.http.routers.opentracker.tls.certresolver: letsencrypt
      traefik.http.services.opentracker.loadbalancer.server.port: 6969
      homepage.group: Containers
      homepage.name: OpenTracker
      homepage.icon: /icons/opentracker.png

networks:
  apps:
    internal: false
  monitoring:
    internal: true
  watchtower:
    internal: true
  secret:
    internal: true
  vault:
    internal: true
  gokapi:
    internal: true
  chrome:
    internal: true
  notes:
    internal: true
  cobalt_api:
    internal: true
  speedtest:
    internal: true
  it-tools:
    internal: true
  pdf:
    internal: true
  shinobi:
    internal: true
  opentracker:
    internal: true

volumes:
  traefik_data:
    external: true
  traefik_certs:
    external: true

  homepage:
    external: true
  homepage_images:
    external: true
  homepage_icons:
    external: true

  nextcloud_html:
    external: true
  nextcloud_php:
    external: true

  postgres_data:
    external: true

  redis_data:
    external: true

  pgadmin:
    external: true

  mariadb:
    external: true

  prometheus_etc:
    external: true

  grafana_lib:
    external: true
  grafana_provisioning:
    external: true

  secret:
    external: true

  vault:
    external: true

  gokapi_config:
    external: true
  gokapi_data:
    external: true

  notes_html:
    external: true
  notes_logs:
    external: true
  notes_uploads:
    external: true
  localstack:
    external: true
  localstack_etc:
    external: true
  mysql:
    external: true
  mysql_import:
    external: true

  cobalt_api_cookies:
    external: true
  cobalt_api_services:
    external: true

  stirling_tessdata:
    external: true
  stirling_configs:
    external: true
  stirling_logs:
    external: true
  stirling_pipeline:
    external: true

  shinobi:
    external: true
  shinobi_custom_auto_load:
    external: true
  shinobi_config:
    external: true
  shinobi_plugins:
    external: true
  shinobi_database:
    external: true

  homeassistant:
    external: true

  immich-model-cache:
    external: true